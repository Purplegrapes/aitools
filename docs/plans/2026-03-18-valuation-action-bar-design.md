# 估值工具底部操作栏统一设计

## 目标

- 统一估值工具底部操作栏的主次层级和点击体验
- 将底部栏容器、主按钮、次级按钮收敛为 `uno.config.ts` 语义类

## 设计结论

- 底部栏统一采用“强主按钮型”
- 左侧为 1 到 2 个次级入口，浅底弱化
- 右侧为 1 个主按钮，使用品牌色实心强调
- 所有底部栏统一高度、圆角、间距和安全区处理

## 语义类

- `vt-action-bar`
- `vt-action-secondary`
- `vt-action-primary`
