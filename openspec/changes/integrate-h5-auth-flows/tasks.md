## 1. API 与认证状态基线梳理

- [ ] 1.1 对照 Swagger 与现有 `src/subPages/tamp/api/index.ts`，补齐 `code` 换 token、验证码发送、手机号验证码登录 Method 定义
- [ ] 1.2 统一认证响应类型与错误映射，确保中转页与登录页可复用同一结果结构
- [ ] 1.3 增加统一登录态写入动作（store action 或 composable），集中处理 token、isLogin 与必要用户信息写入

## 2. 外部小程序统一入口网关

- [ ] 2.1 新增统一中转 URL 构造规则（目标 `subPages` 路径 + referer 编码），用于所有外部小程序入口
- [ ] 2.2 盘点现有外部小程序跳转 `subPages` 的触达点并统一改为先进入 TAMP 中转页
- [ ] 2.3 在目标页增加兜底检测：发现外部来源但缺少中转上下文时，回跳到 TAMP 并保留当前目标路径
- [ ] 2.4 增加中转页 referer 安全校验，仅允许白名单 `subPages` 路径进入重定向
- [ ] 2.5 增加 tools 目标路径分支：命中 `subPages/tools/**` 时由 TAMP 二跳 `subPages/tools/entry` 并透传 embed 参数

## 3. 中转页 code 自动登录改造

- [ ] 3.1 重构 `src/subPages/tamp/index.vue` 外部访问分支，优先执行 `code` 换 token，再兼容 token 直传兜底
- [ ] 3.2 为中转登录流程添加 in-flight 防重保护，避免重复触发认证请求
- [ ] 3.3 实现认证失败分流：H5+loginUrl 外跳、小程序来源回来源、其余跳本地 H5 登录页并保留 referer
- [ ] 3.4 校验成功后 referer 跳转路径，确保 replace 跳转不会回退到中转页

## 4. H5 手机号验证码登录页实现

- [ ] 4.1 新增 H5 登录页面（含手机号输入、验证码输入、发送按钮、登录按钮）并接入路由
- [ ] 4.2 接入发送验证码请求与倒计时节流，覆盖手机号格式校验与禁用态
- [ ] 4.3 接入手机号验证码登录请求，成功后调用统一认证写入动作并执行 referer/默认首页跳转
- [ ] 4.4 登录失败态统一展示（验证码错误、过期、网络异常），避免写入无效 token

## 5. 跨流程一致性与回退验证

- [ ] 5.1 验证“中转自动登录”与“H5 手动登录”共享同一登录态契约（页面鉴权一致）
- [ ] 5.2 验证退出登录后两条路径的登录态同步失效
- [ ] 5.3 验证来源差异化回跳链路（H5 外部、小程序外部、内部访问）
- [ ] 5.4 验证外部小程序进入任意 `subPages` 页面均先经过 TAMP 中转页
- [ ] 5.5 验证外部 tools 目标链路为“TAMP -> tools/entry -> targetUrl”，且 `ticket/sign/traceId` 不丢失

## 6. 回归与交付

- [ ] 6.1 完成关键路径自测清单：code 成功/失败、验证码发送成功/失败、登录成功/失败、重复点击防抖
- [ ] 6.2 执行项目规范检查（至少 `pnpm lint` 与必要的子模块自测）并修复阻断问题
- [ ] 6.3 同步更新 `docs/tools-embed-integration.md`，明确外部统一入口与 tools 二跳标准流程
- [ ] 6.4 更新变更说明中的接口字段假设，记录联调注意事项与已知限制
