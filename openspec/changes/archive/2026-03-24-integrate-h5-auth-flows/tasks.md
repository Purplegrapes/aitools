## 1. API 与认证状态基线梳理

- [x] 1.1 对照 Swagger 与现有 `src/subPages/auth/api/index.ts`，确认验证码发送、手机号验证码登录 Method 定义满足 H5 登录页需求
- [x] 1.2 统一认证响应类型与错误映射，确保验证码登录结果可复用现有认证写入结构
- [x] 1.3 复用或补齐统一登录态写入动作，集中处理 token 与必要用户信息写入

## 2. H5 手机号验证码登录页实现

- [x] 2.1 新增或完善 H5 登录页面（含手机号输入、验证码输入、发送按钮、登录按钮）并接入路由
- [x] 2.2 接入发送验证码请求与倒计时节流，覆盖手机号格式校验与禁用态
- [x] 2.3 接入手机号验证码登录请求，成功后调用统一认证写入动作并执行 referrer/默认首页跳转
- [x] 2.4 登录失败态统一展示（验证码错误、过期、网络异常），避免写入无效 token

## 3. 跨流程一致性与回归验证

- [ ] 3.1 验证 H5 手动登录与现有认证链路共享同一登录态契约（页面鉴权一致）
- [x] 3.2 验证退出登录后共享 token 状态同步失效
- [x] 3.3 验证登录成功后 referrer 回跳与默认首页兜底行为
- [ ] 3.4 完成关键路径自测清单：验证码发送成功/失败、登录成功/失败、重复点击防抖
- [ ] 3.5 执行项目规范检查（至少 `pnpm lint` 与必要的 H5 登录相关自测）并修复阻断问题

## 4. 交付与联调说明

- [x] 4.1 更新变更说明中的接口字段假设，记录联调注意事项与已知限制
